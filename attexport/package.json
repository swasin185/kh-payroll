{
    "name": "keehin-att-data-transfer",
    "version": "1.0.0",
    "description": "Daily data transfer from MS Access (via ADODB) to MariaDB for the KEEHIN application.",
    "main": "attexport.js",
    "scripts": {
        "build": "tsc",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Tom",
    "license": "ISC",
    "dependencies": {
        "dayjs": "^1.11.18",
        "mariadb": "^3.2.3",
        "node-adodb": "^5.0.0"
    },
    "devDependencies": {
        "@types/node": "^24.5.2",
        "typescript": "^5.9.3"
    }
}